
import { Icon } from 'phosphor-react';

export interface Concept {
    id: string;
    title: string;
    what: string;
    why: string;
    how: string;
    code: string;
    icon: Icon;
    lang: string;
}
